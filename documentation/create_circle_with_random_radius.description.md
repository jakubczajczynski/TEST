Creates a circle with a random radius offset.

**Inputs:**
- center (Point3d, item): center point
- base_radius (float, item): base radius value
- random_domain (Interval, item): domain for random offset

**Outputs:**
- circle (Circle): resulting circle
- final_radius (float): actual radius used